import React from 'react';

const GraphViewer = () => {
  return (
    <div>
      {/* Graph will be rendered here */}
      <p>GraphViewer component</p>
    </div>
  );
};

export default GraphViewer;